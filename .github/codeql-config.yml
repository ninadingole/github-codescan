name: "My CodeQL config"

disable-default-queries: true

queries:
  - name: Extended Security
    uses: security-extended
  - name: For Quality
    uses: security-and-quality
  - name: codeql repo
    uses: github/codeql/java@master

paths-ignore:
  - node_modules
  - '**/*.test.js'
paths:
  - src