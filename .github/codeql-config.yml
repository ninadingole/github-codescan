name: "My CodeQL config"

disable-default-queries: true

languages: java

queries:
  - name: Extended Security
    uses: security-extended
  - name: For Quality
    uses: security-and-quality
  - name: codeql repo
    uses: github/codeql/java/ql@v1.24.0

paths-ignore:
  - node_modules
  - '**/*.test.js'
paths:
  - src